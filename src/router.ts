export const build = {
  asteroid: (id: string) => `/asteroid/${id}`,
};

export const path = {
  home: '/asteroids',
  asteroid: '/asteroid/:id',
};

export const route = {
  home: '/asteroids',
  asteroid: '/asteroid/:id',
};
